name: Update views.json

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Fetch traffic
        run: |
          curl -H "Authorization: Bearer ${{ secrets.GH_TOKEN }}" \
               -H "Accept: application/vnd.github+json" \
               https://api.github.com/repos/KamilDziuk/blog/traffic/views \
               > views.json

      - name: Commit and push changes
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add views.json
          git commit -m "Update views" || echo "No changes"
          
         
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/KamilDziuk/blog.git
          
          git push origin main
